const defaultAddItemState = {
  qty: 0,
  price: "0.00",
  total: "0.00"
};

const addItem = (state = defaultAddItemState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default addItem;
